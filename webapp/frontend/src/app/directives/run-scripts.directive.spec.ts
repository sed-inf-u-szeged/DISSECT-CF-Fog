import { RunScriptsDirective } from './run-scripts.directive';

describe('RunScriptsDirective', () => {
  it('should create an instance', () => {
    const directive = new RunScriptsDirective(null);
    expect(directive).toBeTruthy();
  });
});
